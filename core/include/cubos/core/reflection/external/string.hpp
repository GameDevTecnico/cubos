/// @file
/// @brief Declares `std::string` as reflectable.

#pragma once

#include <string>

#include <cubos/core/reflection/reflect.hpp>

/// @brief Declares `std::string` as reflectable.
CUBOS_REFLECT_EXTERNAL_DECL(std::string);
